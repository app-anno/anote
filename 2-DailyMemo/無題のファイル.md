---
created: 
tags: 
related:
---


```dataview
TABLE

file.link as "ファイル",

dateformat(file.ctime, "yyyy/M/d") as "作成日",

list(filter(file.lists, (l) => l.section.heading == "今日優先すること" && l.text != "").text) as "今日優先",

list(filter(file.lists, (l) => l.section.heading == "今週優先すること" && l.text != "").text) as "今週優先",

list(filter(file.lists, (l) => l.section.heading == "小さな成功" && l.text != "").text) as "小さな成功",

list(filter(file.lists, (l) => l.section.heading == "今日やったこと" && l.text != "").text) as "今日やったこと",

list(filter(file.lists, (l) => l.section.heading == "学んだこと・気付き・その他" && l.text != "").text) as "学び・気付き"

FROM "2-DailyMemo"

SORT file.ctime DESC
```

